docker network create caso-testee

docker build -t test-shell --no-cache .

docker build -t test-shell --no-cache .

docker run --network app -d --name app -p 3000:3000 app

docker run --network app -d --name test-shell test-shell